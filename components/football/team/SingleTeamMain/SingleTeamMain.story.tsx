import type { Meta } from '@storybook/react';
import { SingleTeamMain } from './SingleTeamMain';

const meta: Meta = {
  component: SingleTeamMain,
};

export default meta;

export const Default = meta;
