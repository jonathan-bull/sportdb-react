import type { Meta } from '@storybook/react';
import { SingleTeamColours } from './SingleTeamColours';

const meta: Meta = {
  component: SingleTeamColours,
};

export default meta;

export const Default = meta;
